// GitHub Contents API client from the first prototype has been intentionally retired.
// The current app uses a local Git working tree and Tauri/Rust git commands so that
// new files, folders, renames, deletes, commits and pushes all behave like normal Git.
export {};
