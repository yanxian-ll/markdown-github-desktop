# v0.10.15 unified bottom dock

- 合并“问题 / 输出 / 日志”和 AI 证据写作面板为统一底部 Dock，避免两个底部浮层重叠。
- 底部 Dock 标签包含：问题、输出、日志、AI 对话、证据库、Diff 审批、模型。
- 从底部拖动热区默认拉出 AI 对话；状态栏的“问题”和“AI”只切换统一底部 Dock 的标签页。
- 构建出错时，如果用户正在 AI 标签页，不强制切走，只更新问题数量；否则打开问题标签。
- 保存底部 Dock 高度、展开状态和当前标签页。
