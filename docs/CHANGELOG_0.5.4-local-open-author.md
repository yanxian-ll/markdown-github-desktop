# 0.5.4 本地打开与批注作者优化

- 文档栏只保留一个“打开本地文档”入口，不再常驻展开“打开文件夹 / 打开文件”两个按钮。
- 由于跨平台系统文件选择器无法稳定在同一个原生对话框里同时选择文件和文件夹，点击入口后用轻量确认选择“文件夹”或“单个文件”。
- 本地文件/本地文件夹批注的作者统一使用设置里的“用户名”。只要填写用户名，本地批注也会显示该名称。
- 设置里的用户名会独立持久化，不再依赖当前是否是 GitHub 工作区。
