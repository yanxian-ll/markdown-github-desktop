# v0.3.0

## 性能与打开文件优化

- 快速连续点击文件时，采用打开任务序号，只允许最后一次点击更新当前文档、PDF 预览、状态栏和错误信息。
- 上一个文件的异步读取完成后，如果用户已经点击了新文件，结果会被丢弃，不再覆盖当前界面。
- PDF 预览增加加载任务和渲染任务取消逻辑：切换 PDF、切换页码或缩放时，会取消旧的 pdf.js 渲染任务。
- LaTeX 构建完成后，如果用户已经切换到其他文件，不再自动覆盖右侧 PDF 预览。
- `busy` 状态只由最新打开任务释放，避免旧任务结束导致状态闪烁。
