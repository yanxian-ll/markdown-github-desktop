# Changelog 0.2.5

- 左侧目录树的顶部 `＋` 改为上下文感知：未选中时在根目录创建，选中文件夹时在该文件夹创建，选中文件时在其父目录创建。
- 新增目录树选中态，点击文件或文件夹即可选中。
- 支持将文件或文件夹拖动到目标文件夹移动位置。
- 支持将文件或文件夹拖动到左侧空白区域移动到工作区根目录。
- 保留节点右侧 `✎` 重命名和 `×` 删除操作。
- Git clone / pull 后继续自动刷新左侧目录树。
