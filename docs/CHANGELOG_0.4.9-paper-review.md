# v0.4.9-paper-review

- 修正 TeX -> PDF 正向定位的视觉提示：SyncTeX 坐标只作为滚动锚点，不再直接信任 `W/H` 绘制大框。
- 正向定位高亮优先吸附到 pdf.js text layer 中离 SyncTeX 点最近的真实文字矩形，避免跨栏、偏到文字外或覆盖过宽。
- 如果附近没有可用文字层矩形，则只显示紧凑的锚点高亮，不再画大范围区域。
- 正向定位高亮改为短暂闪烁后自动消失。
