# v0.9.9 预览导出收敛与文档树定位

- 预览区顶部移除独立“构建 PDF”按钮，Markdown 的 PDF 生成统一放入“导出 → PDF”。
- 保留 `Ctrl/Cmd+B` 快捷键和编辑器内部构建事件，避免影响已有键盘工作流。
- 文档树新增打开文件后的自动定位逻辑：当前文件会高亮，父级文件夹会自动展开。
- 大纲跳转、引用跳转、BibTeX 跳转、模板创建后自动打开、每日笔记打开等流程都会同步文档树选中状态。
- 保存临时文档为工作区文件后，会刷新目录并选中新保存的文件。
