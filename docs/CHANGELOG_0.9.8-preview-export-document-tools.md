# v0.9.8 Preview export and document-area writing tools

- Moved Markdown export actions out of the editor side panel and into the preview header next to the Pandoc PDF build action.
- Removed the large Project Tools side panel from the editor header; the previous research/publish/framework/writing tabs no longer occupy editor space.
- Added a daily-note shortcut to the document tree header so writing notes live with the project file structure.
- Removed the duplicate "hide document tree" button from the document tree header; the top toolbar remains the single document-tree visibility control.
