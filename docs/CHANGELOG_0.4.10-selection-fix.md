# v0.4.10-selection-fix

- 修复 PDF 文本选择批注在双栏论文中错误跨栏的问题。
- PDF 文字批注不再直接使用 `Range.getClientRects()` 的大范围矩形，而是基于 pdf.js text layer 的真实文字 span 生成批注锚点。
- 鼠标拖拽起点和终点都在同一栏时，会自动过滤另一栏误选文本。
- 选中后立即清除浏览器原生跨栏蓝色选区，改为显示软件计算后的干净选区预览。
- 保留原有右键“批注”、PDF→TeX、TeX→PDF 和批注栏筛选逻辑。
