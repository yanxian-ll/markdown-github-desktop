# v0.2.9

- PDF 预览改为按页渲染，避免打开大型论文 PDF 时一次性渲染所有页面导致卡顿。
- PDF 预览支持上一页/下一页、放大、缩小、重置缩放。
- 图片预览支持放大、缩小、重置缩放。
- 文档树、编辑器/预览、设置栏之间加入拖拽分隔条，可手动调整每栏宽度。
- 打开 `.tex` 时优先查找同名已构建 PDF 并直接显示；未找到 PDF 时不再反复自动重编译，提示使用 Ctrl/Cmd+B 构建。
- SyncTeX 正向定位不再自动触发构建；未找到 PDF 时会提示先构建。
